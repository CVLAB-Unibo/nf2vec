# IMPORTANT: Use relative paths to the root of the project or absolute paths

inrs_root: "task_mapping_network/inrs"
ckpt_path: "task_mapping_network/inr2vec/train/299.pt"
out_root: "task_mapping_network/inr_embeddings"

nerf2vec_ckpt_path: "nerf2vec/train/ckpts/499.pt"
nerf2vec_train_json_path: "data/train.json"
nerf2vec_val_json_path: "data/val.json"
nerf2vec_test_json_path: "data/test.json"
nerf_out_root: "task_mapping_network/nerf_embeddings"

encoder:
  hidden_dims: [512, 512, 1024, 1024]
  embedding_dim: 1024

mlp:
  hidden_dim: 512
  num_hidden_layers: 4

train_split: "train"
val_split: "val"
test_split: "test"
